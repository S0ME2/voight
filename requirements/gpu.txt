# Server-only GPU runtime. Keep this file separate from cpu.lock; do not
# install it in the local development environment.
# Docker installs the full pinned closure without dependency resolution.
-r gpu.lock
